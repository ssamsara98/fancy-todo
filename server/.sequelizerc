// .sequelizerc
const path = require("path");

const source = process.env.NODE_ENV !== "production" ? "src" : "dist";

module.exports = {
  config: path.resolve("config", "database.js"),
  "models-path": path.resolve(source, "models"),
  "seeders-path": path.resolve(source, "db", "seeders"),
  "migrations-path": path.resolve(source, "db", "migrations"),
};
